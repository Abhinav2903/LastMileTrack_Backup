import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lastmiletrack.app',
  appName: 'lastMileTrack',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
